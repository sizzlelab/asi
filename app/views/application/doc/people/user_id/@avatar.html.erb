<h1><code>/people/&lt;user_id&gt;/@avatar</code></h1>
<p>
  A resource for accessing a user's avatar image.
</p>

<p>PUT method requires that the user with <tt>user_id</tt> is logged in &ndash; otherwise, 
<em>403 forbidden</em> is returned.</p>

<% methods = [ 
                { :name => "GET", 

                 :description => "Gets the full sized avatar image of the user. Maximum width is 600px and maximum height 800px."},                
               
               {
                 :name => "PUT",
                 :description => "Update (or add) an avatar to a user specified by <tt>user_id</tt>.",
                 :parameters => [{
                                    :file => [
                                            	{:content_type  => "Content type of user's avatar image."},
                                            	{:filename => "Filename of user's avatar image file."}
                                          	 ]
                                }]
               } ] %>

<%= render :partial => "doc/methods", :locals => { :methods => methods } %>
